flask>=3.0,<4.0
jinja2>=3.1,<4.0

# Azure Key Vault integration (write side)
azure-identity>=1.15,<2.0
azure-keyvault-secrets>=4.7,<5.0

# Azure ARM management plane (vault enumeration + provisioning for the
# multi-vault picker - lets the onboarder show the user a live list of
# Key Vaults they have access to, and provision new ones via the form).
azure-mgmt-keyvault>=10.3,<11.0
azure-mgmt-resource>=23.0,<24.0
azure-mgmt-subscription>=3.1,<4.0
azure-mgmt-authorization>=4.0,<5.0
